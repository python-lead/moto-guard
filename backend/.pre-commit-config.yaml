repos:
  - repo: local
    hooks:
      - id: isort
        name: isort
        entry: isort backend/src --profile", "black", "--filter-files
        language: system
        types: [ python ]

      - id: black
        name: black
        entry: black backend/src
        language: system
        types: [ python ]
